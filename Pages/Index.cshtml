@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Book Application</h1>
    <p>....</a>.</p>
</div>

@foreach (var book in Model.books)
{
    <h2>Title: @book.Title. Author: @book.Author</h2>
    <br />
}
